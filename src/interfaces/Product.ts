export default interface Product {
    id: number;
    brand_id: string;
    external_id: string;
    title: string;
    description: string;
    product_page_url: string;
    image_url: string;
    category_id: number;
    product_id: number;
}
